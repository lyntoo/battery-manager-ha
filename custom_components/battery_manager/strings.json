{
  "config": {
    "step": {
      "user": {
        "title": "Battery Manager",
        "description": "Set up the Battery Manager panel."
      }
    },
    "abort": {
      "already_configured": "Already configured. Only a single instance is allowed."
    }
  }
}
